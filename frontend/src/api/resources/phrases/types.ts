import { components } from '../../schema';

export namespace Phrases {
  export type Phrase = components['schemas']['Phrase'];

  export type PatchedPhrase = components['schemas']['PatchedPhrase'];
}
